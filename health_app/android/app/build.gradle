// plugins {
//    id "com.android.application"
   //  id "org.jetbrains.kotlin.android"
//    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
//    id "dev.flutter.flutter-gradle-plugin"
// }

apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'

android {
    namespace = "com.example.health_app"
    // Use hardcoded values for the SDK and other configurations
    compileSdk = 34  // Update to the version you want

    ndkVersion = "21.4.7075529"  // Example NDK version, adjust as needed

    sourceSets {
        main.java.srcDirs += 'src/main/kotlin'
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.health_app"
        minSdkVersion = 21  // Example value; adjust to your needs
        targetSdkVersion = 34  // Align with compileSdk
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

apply from: "/home/victor/flutter/packages/flutter_tools/gradle/flutter.gradle"

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"
}
flutter {
    source = "../.."
}
